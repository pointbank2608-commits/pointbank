# 클래스뱅크 파닉스 워크시트 — 커서용 디자인 지시서

작성: Claude Code. 이 문서는 **이미지 제작 전용** 지시서다. 코드·SQL·i18n 수정, 커밋·푸시·배포는 하지 않는다(이미지 제작 후 사용자가 Claude Code에 요청하면 Claude가 코드에 연결한다).

## 1. 무엇을 만드나

유치원~초등 저학년(만 4~8세)이 쓰는 **파닉스 워크시트를 귀엽게 꾸며 줄 그림 부품**을 만든다. 코드로 그린 기본 스타일(둥근 카드, 리본 제목, 별 칸)은 이미 있고, 여기에 **마스코트 캐릭터·리본·스티커·테두리 장식**이 얹힌다.

- 지금 모습 확인: 앱에서 `수업 자료실 → 파닉스 워크시트`(`/materials/phonics`)에서 단어를 골라 유형을 누르면 나온다. 머리글의 해·구름, 바닥의 꽃, "Sticker" 점선 원이 **그림으로 바뀔 자리**다.
- 인쇄 환경: 학원 프린터 — **컬러 인쇄와 흑백 인쇄가 둘 다 흔하다.** 그래서 부품마다 **컬러판**과 **흑백 선화판(`-bw`)** 두 벌이 필요하다.

## 2. 화풍 (모든 부품 공통)

- **견본**: `app/scripts/vocab/ref/lineart-style-sample.webp` 의 굵은 선, 둥글고 통통한 비율, 단순한 형태, 귀여운 미소. 컬러판은 이 화풍에 밝은 색을 칠한 느낌(평면 벡터풍, 그림자·질감 최소).
- **선**: 순검정에 가까운 짙은 선, 굵고 균일, 끝은 둥글게. 모든 칸은 **닫힌 도형**(색칠·인쇄 시 번지지 않게).
- **표정**: 눈은 동그랗고 크게, 입은 작은 미소. 무섭거나 뾰족한 표현 금지.
- **색(컬러판) 팔레트** — 코드가 쓰는 색과 어울리게:
  - 하늘 파랑 `#3F97E0`, 해 노랑/주황 `#F2A516`, 초록 `#48B56B`, 분홍 `#EE6F9A`, 보라 `#8B6FD6`
  - 연한 바탕색: `#E8F3FD`, `#FFF5D6`, `#E5F6EB`, `#FDE8F0`, `#EEE8FA`
  - 캐릭터 몸은 이 팔레트 안에서 고르되 배경 카드(파스텔)와 구분되도록 채도를 조금 더 높인다.
- **흑백판(`-bw`)**: 같은 그림을 **흰색 채움 + 검은 윤곽선만**으로. 회색·그라데이션 금지(흑백 프린터에서 뭉개진다). 눈·코 같은 작은 부분만 검게 채운다.
- **글자·숫자·로고 절대 금지.** 그림 안에 어떤 글자도 넣지 않는다(리본·말풍선도 **비워 둔다**. 글자는 앱이 얹는다).
- **저작권**: 기존 캐릭터(디즈니·산리오 등)나 다른 사이트 그림을 따라 그리지 않는다. 순수 창작 캐릭터만.

## 3. 파일 규격

- 저장 위치: `app/public/worksheet-cute/` (폴더가 없으면 만든다).
- 파일명: 아래 표의 `id` 그대로 `<id>.webp`(컬러), `<id>-bw.webp`(흑백).
- 형식: **배경이 투명한 WebP**(알파 채널 필수 — 파스텔 카드 위에 올라간다). 투명 배경이 어려우면 PNG로 만들고 알려 달라(변환은 Claude가 한다).
- 크기: 표에 적은 픽셀. 여백은 대상 둘레에 3% 정도만.
- 용량: 장당 200KB 이하 권장.

## 4. 만들 목록

### 4-1. 마스코트 (가장 중요 — 먼저 만든다)

**Ollie(올리) — 아기 올빼미.** 둥글둥글한 몸, 큰 동그란 눈, 작은 부리, 머리에 뾰족한 깃 두 개, 작은 날개. 책과 연필을 좋아하는 "읽기 선생님" 같은 친구. 몸 색은 하늘 파랑 계열 + 배는 연한 크림.

**Pip(핍) — 병아리.** 노랑 둥근 몸, 작은 주황 부리와 발, 정수리에 깃 세 가닥. Ollie의 짝꿍이고 "다시 해 볼까?" 같은 격려 담당.

| id | 크기 | 포즈 |
|---|---|---|
| `ollie-wave` | 1024×1024 | 손 흔들며 인사 |
| `ollie-cheer` | 1024×1024 | 두 날개를 번쩍 든 응원 |
| `ollie-think` | 1024×1024 | 날개로 턱을 괴고 생각 |
| `ollie-point` | 1024×1024 | 날개로 옆을 가리킴(말풍선·문제를 가리킬 때) |
| `ollie-read` | 1024×1024 | 커다란 책을 펼쳐 듦(책 표지·페이지에 글자 없음) |
| `ollie-star` | 1024×1024 | 큰 별을 안고 있음(칭찬용) |
| `ollie-thumbsup` | 1024×1024 | 엄지 척 |
| `pip-hello` | 1024×1024 | 갸우뚱 인사 |
| `pip-happy` | 1024×1024 | 눈 감고 활짝 웃음 |
| `pip-cheer` | 1024×1024 | 폴짝 뛰며 응원 |
| `pip-oops` | 1024×1024 | 머쓱하게 머리를 긁적(틀려도 괜찮아 — 절대 슬프거나 울지 않게) |
| `pip-peek` | 1024×1024 | 화면 모서리에서 빼꼼 내다봄(아래쪽 절반만 보이는 구도) |

각각 컬러판 + `-bw` 흑백판 = 24장.

### 4-2. 제목 리본 (머리글의 제목 뒤에 깔린다)

가로로 긴 둥근 리본(양 끝이 접힌 모양). **가운데는 비워 둔다**(제목 글자가 들어감).

| id | 크기 | 색 |
|---|---|---|
| `ribbon-blue` | 1600×400 | 하늘 파랑 |
| `ribbon-yellow` | 1600×400 | 노랑 |
| `ribbon-green` | 1600×400 | 초록 |
| `ribbon-pink` | 1600×400 | 분홍 |
| `ribbon-bw` | 1600×400 | 흑백판(흰 채움 + 굵은 윤곽선) |

### 4-3. 스티커 (바닥의 "Sticker" 자리·칭찬 표시)

| id | 크기 | 내용 |
|---|---|---|
| `sticker-star` | 512×512 | 통통한 별(웃는 얼굴) |
| `sticker-heart` | 512×512 | 하트(웃는 얼굴) |
| `sticker-thumbsup` | 512×512 | 엄지 척 손 |
| `sticker-medal` | 512×512 | 리본 달린 동그란 메달(가운데 별) |
| `sticker-sun` | 512×512 | 웃는 해 |
| `sticker-flower` | 512×512 | 웃는 꽃 |
| `sticker-rainbow` | 512×512 | 구름 위 무지개 |
| `sticker-balloon` | 512×512 | 풍선 세 개 묶음 |

각각 컬러 + `-bw` = 16장.

### 4-4. 말풍선 (빈 풍선, 글자는 앱이 넣는다)

| id | 크기 | 모양 |
|---|---|---|
| `bubble-round` | 800×600 | 둥근 말풍선(꼬리 왼쪽 아래) |
| `bubble-cloud` | 800×600 | 구름 모양 생각 풍선 |
| `bubble-star` | 800×600 | 가장자리가 톡톡 튀어나온 별 모양 풍선 |

각각 컬러(흰 채움 + 색 테두리) + `-bw` = 6장.

### 4-5. 페이지 테두리·모서리 장식

| id | 크기 | 내용 |
|---|---|---|
| `frame-a4` | 2480×3508 | A4 세로 한 장 크기. **가운데는 완전히 투명**, 가장자리 약 12mm 폭에 꽃·별·잎 덩굴이 이어진 테두리 |
| `frame-a4-bw` | 2480×3508 | 흑백판 |
| `corner-flowers` | 512×512 | 모서리에 놓는 꽃 묶음(좌상단 기준 구도) |
| `corner-stars` | 512×512 | 모서리에 놓는 별·반짝이 묶음(좌상단 기준 구도) |

`corner-*` 는 컬러 + `-bw` = 4장.

**합계**: 24 + 5 + 16 + 6 + 2 + 4 = **57장**.

## 5. 진행 방식

1. **먼저 `ollie-wave`, `ollie-cheer`, `pip-hello` 컬러판 3장(+ 각 `-bw`)만 만들고 멈춰서 사용자에게 캐릭터 모습을 확인받는다.** 캐릭터가 확정돼야 나머지 포즈가 일관된다.
2. 승인되면 마스코트 나머지 → 리본 → 스티커 → 말풍선 → 테두리 순으로, 한 묶음을 끝낼 때마다 만든 개수와 건너뛴 id를 보고한다. 이미 만든 파일은 다시 만들지 않는다.
3. 캐릭터의 외형(색·비율·표정)은 **모든 포즈에서 똑같이** 유지한다. 포즈만 바뀐다.

## 6. 하지 말 것

- 코드, SQL, i18n, 기존 이미지(`word-bank-images`, `word-bank-lineart`, `phonics-images`) 수정 금지 — 새 이미지 파일 추가만.
- 표에 없는 id로 파일 이름 짓기 금지.
- 그림 안에 글자·숫자 넣기 금지.
- 커밋·푸시·배포 금지.

## 7. 코드에 연결되는 자리 (참고 — Claude가 연결한다)

| 부품 | 들어갈 자리 |
|---|---|
| `ribbon-*` | 머리글 제목 뒤(지금은 파란 알약 모양) |
| `ollie-*`, `pip-*` | 머리글 옆, 안내 문구 옆, 정답 칭찬 자리 |
| `sticker-*` | 바닥 왼쪽·오른쪽(지금은 꽃 선화와 "Sticker" 점선 원) |
| `bubble-*` | 마스코트가 안내 문구를 말할 때 |
| `frame-a4`, `corner-*` | 페이지 테두리(지금은 파란 점선) |
| `-bw` 판 | 워크시트 화면의 "컬러로 인쇄" 체크를 끄면 자동으로 사용 |
