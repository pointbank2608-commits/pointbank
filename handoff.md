# 프로젝트 핸드오프 — 학원용 포인트 뱅킹 & 미니게임 웹앱

## 한 줄 요약
공부방/학원에서 쓸 웹앱. (1) 학생별 포인트 계좌를 만들어 선생님이 지급/차감하고 순위를 보여주는 "포인트 뱅킹" 서비스, (2) 룰렛/사다리/빙고 같은 미니게임 서비스, 이 두 개를 하나의 사이트 안에서 쓸 수 있게 만드는 것이 목표.

## 지금까지 진행된 것
- ~~포인트 뱅킹 HTML 프로토타입~~ → **React + Vite + Supabase 실제 서비스로 전환 완료** (`app/` 폴더).
  - 이메일/비밀번호 로그인, 원장/선생님/학생 3역할, 학원·반 단위 데이터 분리(RLS), 거래 원장 기반 포인트 지급/차감, 반별·학원 전체 순위까지 동작.
  - `point-bank-prototype.html` 은 참고용으로만 남겨둠. 디자인 톤은 그대로 계승했음.
- **남은 필수 작업 1개**: `supabase/schema.sql` 을 Supabase 대시보드 SQL Editor 에서 실행해야 함. 아직 안 돌렸음 (README 참고).
- **게임 카테고리 구조 도입** (2026-08-17). 네비게이션엔 "게임" 버튼 하나만 있고
  (설정 오른쪽, 맨 끝), 누르면 `/games` 목록 페이지가 뜨고 카드를 눌러 개별 게임으로 들어감.
  새 미니게임을 추가할 때마다 네비게이션에 메뉴를 늘리지 않고 `GamesPage.tsx` 의 `GAMES`
  배열에 카드 하나만 추가하면 됨. **앞으로 만드는 게임은 전부 이 구조를 따를 것.**
- **미니게임 1호: 돌림판(룰렛) 구현 완료** (2026-08-17). 사다리/빙고는 아직.
  - `game_templates` 테이블 그대로 사용 (스키마 변경 없음). `game_type='wheel'`, `items: {id,label}[]`.
  - 반 전용(`class_id` 지정) / 학원 전체 공용(`class_id = null`) 두 범위 지원. 만들 때 선생님이 선택.
  - 선생님·원장만 항목 추가/삭제/이름변경/삭제 가능(RLS `is_staff()` 그대로 적용됨, 추가 정책 불필요).
  - 학생도 접근 가능 — 보되 수정은 못 함. `/games/wheel` 경로, `/games` 목록에서 진입.
  - SVG로 직접 그림(라이브러리 없음). 회전각 계산은 "12시=0도, 시계방향" 각도계 기준으로
    누적 회전값에 델타를 더하는 방식이라 여러 번 돌려도 자연스럽게 이어짐 (0으로 스냅 안 됨).
    `app/src/lib/wheel.ts` 의 `computeSpinRotation` 참고. 여러 항목수 x 여러 목표값으로
    시뮬레이션 검증 완료.
  - "당첨된 항목 제거하며 진행" 토글 — 저장된 템플릿을 건드리지 않고 그 판(session)에서만
    항목을 빼는 방식. 발표자 뽑기처럼 중복 없이 뽑아야 하는 용도.
- **베타 오픈 전 보안 점검 → 학생 로그인 잠금** (2026-08-24경). 초대코드/학생코드가 같은 학원
  소속이면 누구나 조회 가능한 구조라, 이메일 인증까지 꺼둔 베타 세팅에서는 학생 계정으로 선생님
  권한을 탈취하거나 남의 학생 계정을 가로챌 수 있는 경로가 실제로 있었음. 원장/선생님만 로그인
  가능하도록 잠그는 것으로 해결(`006_disable_student_login.sql`). 이 작업 중 "Supabase는 함수
  생성 시 `anon` 롤에도 기본 EXECUTE 권한을 자동 부여한다"는 함정을 발견 — `authenticated`/`public`만
  revoke하고 `anon`을 빼먹으면 여전히 뚫려 있음. 아래 관리자 기능에도 동일하게 적용함.
- **PWA — 홈 화면에 추가 지원** (2026-08-26경). Safari/iOS(manifest 무시, `apple-mobile-web-app-*`
  메타 태그 필요) / Chrome / 카카오톡 인앱 브라우저(설치 자체가 불가 → "다른 브라우저로 열기" 안내)
  세 갈래로 분기하는 `InstallBanner` 컴포넌트 + `manifest.webmanifest` + 아이콘 3종(192/512/apple-touch-180) 추가.
- **랜딩 페이지** 신규 제작. class5.co.kr 구조 참고, 컬러 섹션 밴드 + 목업 프레임 구성. 목업 속 학생
  이름은 반드시 가상 이름 사용(실제 학생 정보 노출 금지).
- **플랫폼 관리자 페이지** (2026-08-27). "가입한 학원 목록, 학생/선생님 수, 학원 삭제 — 특정 관리자
  계정(`likesea85@naver.com`)만 접근 가능"하게 구성. `profiles.role`에 `'admin'` 추가(academy_id는
  null), `claim_admin()` RPC가 서버에서 `auth.users.email`을 재확인 후 admin 프로필을 부여(클라이언트가
  스스로 admin을 자칭할 수 없음). `admin_list_academies()`(학원별 원장/선생님/학생 수 집계),
  `deleteAcademyAsAdmin`(학원 완전 삭제, cascade), 관리자 비밀번호 변경 기능 포함.
  `is_platform_admin()`을 이용한 추가 permissive RLS 정책을 `academies` 테이블에 얹는 방식(기존
  학원별 정책은 건드리지 않고 OR로 겹쳐짐). 마이그레이션: `supabase/007_platform_admin.sql`.
  사용량 통계(유료화 대비)는 무료 테스트 기간이라 보류 — 요청 시 나중에 추가.
- **미니게임 확장: 사다리타기 · 순서정하기 + 게임 배경음악** (2026-08-28). 요청 원문: "게임은
  우선 돌림판, 사다리, 순서정하기 같은 간단 게임, 단어를 이용한 게임, 문장 만들기 게임 정도로
  우선 만들거야. 간단게임부터 만들자. 게임을 진행할때 음악도 넣을 수 있어?" → 간단 게임 두 개를
  한 번에 만들고, 음악은 "기본 제공 + 업로드 둘 다"로 정함.
  - `game_templates`에 `config jsonb` 컬럼 추가(마이그레이션 `008_ladder_order_games.sql`). 사다리는
    `config.results`(결과 라벨), 모든 게임은 `config.music`(선택한 배경음악)을 여기 저장. 스키마
    변경 없이 앞으로 게임이 늘어나도 이 컬럼 하나로 확장하는 구조.
  - 사다리 생성: `app/src/lib/ladder.ts`. 같은 행에서 인접한 가로줄(rung)을 동시에 놓지 않으면
    위→아래 경로가 항상 1:1 대응(순열)이 된다는 아미다쿠지의 성질을 이용. Node.js로 2~10명 ×
    2만 회 시뮬레이션해서 전부 유효한 순열임을 확인 후 반영(돌림판 회전각 검증 때와 같은 방식).
  - 배경음악은 실제 음원 파일을 서비스에 넣지 않았음 — "방구차 음악" 같은 특정 대중가요/캐릭터
    음악은 저작권이 있어 기본 제공할 수 없기 때문. 대신 (1) Web Audio API로 그 자리에서 합성하는
    효과음 4종(두구두구/짜잔/카운트다운/딩동, `app/src/lib/gameMusic.ts`)을 기본 제공하고,
    (2) 선생님이 원하는 mp3를 직접 업로드하는 기능(`game-audio` 공개 버킷, 학원별 폴더 격리,
    RLS로 업로드/삭제는 선생님만·재생은 같은 학원 로그인 사용자만)을 같이 넣어서 해결.
  - 돌림판(`WheelPage`)·사다리(`LadderPage`)·순서정하기(`OrderPage`)가 반 선택/템플릿 CRUD 로직을
    거의 그대로 공유하길래 `app/src/lib/useGameTemplates.ts` 훅으로 뽑아 중복 제거함
    (돌림판도 이 기회에 같이 리팩터링함 — 처음엔 새 게임 둘만 훅을 썼다가 뒤늦게 맞춤).
- **게임 참가자를 학생 명단에서 클릭으로 추가** (2026-08-28). "게임을 실행할 때마다 이름을 계속
  적어야 해서 불편하다"는 피드백 → 반별/학원별로 이미 등록된 학생 명단에서 클릭 한 번으로(또는
  "전체 추가"로 한 번에) 게임 항목에 넣을 수 있게 함. 세 게임(돌림판·사다리·순서정하기) 공통으로
  들어감 — `useGameTemplates` 훅이 `roster`(현재 스코프의 학생 목록) · `rosterScope`
  ('class'|'academy') · `rosterLoading`을 제공하고, `StudentRosterPicker` 컴포넌트가 그 목록을
  보여주며 이미 추가된 이름은 목록에서 숨긴다. 학원 전체 조회는 `fetchStudentsOfAcademy()` 추가
  (RLS는 기존 `students_select` 정책 그대로 재사용 — 같은 학원이면 이미 전체 학생 명단을 볼 수
  있었으므로 별도 마이그레이션 불필요).
- **미니게임 4호: 시한폭탄(폭탄 돌리기)** (2026-08-28). 참가자를 등록하고 폭발 시간 범위(초)를
  정해두면, 그 사이 무작위 시각에 터지는 숨겨진 타이머가 걸린다. "다음 사람에게 넘기기"로 폭탄을
  돌리다가 터지는 순간 들고 있던 사람이 걸리는, 실제 폭탄 돌리기 파티게임을 그대로 옮긴 것.
  `game_type='bomb'`도 다른 게임과 마찬가지로 `game_templates`를 그대로 쓰고, `config.bombRange
  = {min, max}`(초)만 추가로 저장 — `game_type` 컬럼에 CHECK 제약이 없어서 스키마 변경 없이 새
  게임 종류를 추가할 수 있음을 이번에 실제로 확인함 (SQL 마이그레이션 불필요).
  타이머는 서버 폴링 없이 `setTimeout` 한 번으로 처리(`TimeBomb.tsx`) — 시작 시 min~max 사이
  무작위 지연을 한 번만 계산해 걸어두고, 그 시각에 상태를 'exploded'로 바꾸는 식이라 정확히 언제
  터질지는 선생님도 미리 알 수 없다(진짜 숨겨진 타이머).
- **게임 배경음악과 별도로 "결과 사운드" 도입** (2026-08-28). 지금까지는 결과 확정 시 "짜잔"이
  하드코딩돼 있었는데, `config.resultSound`를 신설해 배경음악과 독립적으로 고를 수 있게 함. 한
  번도 안 건드린 템플릿은 `resolveResultSound()`가 기본값 "짜잔"을 돌려주고, 선생님이 "없음"을
  직접 고르면 그때부터 진짜 무음(`undefined` vs `null`을 구분해서 처리). 이 작업 중 돌림판만
  `GameMusicPicker` 자체가 빠져 있어 배경음악을 고를 방법도, 재생될 방법도 없었던 버그를 같이
  발견해서 고침. 효과음도 시계 초침/박수 갈채/펑(폭발) 3종을 추가함(`app/src/lib/gameMusic.ts`).
- **시한폭탄: 이미지 확대 + "타이머만" 모드** (2026-08-28). 폭탄 이모지가 큰 화면 대비 너무
  작다는 피드백 → 300px 박스/200px 이모지로 확대. 참가자 없이 카운트다운+폭발 연출만 쓰는
  "타이머만" 모드도 추가(로컬 토글, 비영속 — 사다리의 "한 번에/한 명씩" 토글과 같은 방식). 실제
  물건(인형·공 등)을 돌리며 앱은 숨겨진 타이머 용도로만 쓸 때 필요해서 요청받음.
- **미니게임 5호: 타이머 맞추기** (2026-08-28). 목표 시간(초)을 정해두고 "시작"→"멈춤"으로
  최대한 가깝게 맞히는 게임. 숫자를 보이게/숨기게(감으로만 맞히는 원래 방식) 고를 수 있고,
  "공용 도전판"(참가자 구분 없음)과 "참가자별 순위"(한 명씩 도전 후 오차 순 순위표) 중에서도
  고를 수 있다 — 둘 다 로컬 토글(비영속), 목표 시간만 `config.targetMs`(ms)로 저장.
  시간 측정은 `performance.now()` + `requestAnimationFrame` 기준이라 탭이 백그라운드로 가지
  않는 한 프레임 속도와 무관하게 정확하다. `game_type='timer'`도 스키마 변경 없이 바로 추가됨.
- **게임 5종 UI/UX 재배치: 게임 화면이 먼저 보이게** (2026-08-28). "사용자가 대부분 30~40대
  성인 여성이고, 처음부터 이미지 없이 텍스트만 보면 어떻게 쓰는지 모른다"는 피드백 → 지금까지는
  반 탭·저장된 목록 칩·"+새로 만들기"·배경음악 설정 같은 관리용 UI가 먼저 나오고 실제 돌림판/
  사다리 그림은 한참 아래에 있었음. 다섯 게임 페이지(Wheel/Ladder/Order/Bomb/TimerMatch) 모두
  구조를 뒤집어서: **뒤로가기 → 게임 이름(크게) → 실제 게임 그림 → (그 아래) 반 탭·목록 칩·
  만들기 폼·설정 패널** 순으로 재배치함. 이미 로드 시 첫 템플릿이 자동 선택되므로(useGameTemplates
  훅), 반/목록을 먼저 고르게 하지 않아도 진입 즉시 뭔가 보여줄 수 있었음. 배경음악/결과 사운드
  픽커도 눈에 잘 안 띄던 상단 자리에서 접힌 설정 패널 안으로 옮김 — 평소엔 안 보이지만 필요할 때
  펼쳐서 찾을 수 있음(사용자가 확인한 절충안: 설정 패널은 여전히 기본 접힘).
  같은 턴에 "만들기만 있고 삭제가 안 보인다"는 피드백도 받음 — 실제로는 삭제 버튼이 있었지만
  화면 맨 아래 접힌 패널 안 작은 텍스트 링크였어서 못 찾은 것. 목록 칩 자체에 삭제(✕) 버튼을
  달아서 반 선택하듯 목록을 고르는 바로 그 자리에서 지울 수 있게 함
  (`useGameTemplates.ts`의 `handleDeleteTemplate`를 `id?: string` 받도록 확장 — 안 주면 기존처럼
  현재 선택된 것을 지움, 넘기면 그 칩을 바로 지움).

## 요구사항 (원래 요청 원문 기준)

### 1. 학원용 포인트 뱅킹 서비스
- 숙제/수업태도 등에 대한 보상으로 선생님이 학생에게 포인트(가짜 화폐 개념)를 지급.
- 기존엔 종이 쿠폰으로 줘서 분실/집계가 번거로웠음 → 은행 시스템처럼 학생마다 "계좌"를 만들어 관리.
- 선생님이 수업 중/후에 포인트를 지급하거나 차감할 수 있어야 함.
- 포인트의 **단위 이름은 학원마다 커스터마이징 가능**해야 함 (예: 별, 달러 등).
- **"총 합계" 버튼**을 누르면 학생별 포인트 합산 후 **순위**를 보여줘야 함.
- **학원별, 반별로 데이터가 구분**되어야 함 (멀티테넌시).

### 2. 미니게임 서비스
- 돌림판(룰렛), 사다리 게임, 빙고 등.
- 각 게임의 **내용(항목)을 선생님이 입력하고 수정**할 수 있어야 함.
- 게임 종류는 **수시로 추가/제거**될 수 있어야 함 (확장 가능한 구조로 설계 필요).

### 3. 통합
- 위 두 서비스를 **하나의 사이트 안에서** 이용할 수 있어야 함.

## 확정된 설계 방향

**역할 구조**: 원장(전체 관리) / 선생님(자기 반 담당) / 학생(선택적 로그인, 조회 위주)

**데이터 모델 (계층 구조)**
```
학원(Academy)
 └ 반(Class)
    └ 학생(Student) — 계좌(Account)
 └ 선생님(Teacher)
```

**포인트 뱅킹은 "거래 원장(ledger)" 방식으로 설계**
- 잔액을 따로 저장하지 않고, 모든 지급/차감 트랜잭션(`transactions`)의 합으로 잔액을 계산.
- 트랜잭션 필드: 금액(delta), 사유, 지급한 선생님, 시각, 반 정보.
- 이유: 나중에 "누가 언제 왜 줬는지" 추적/정정 가능해야 하기 때문.
- 추후 "포인트로 상품 교환" 기능 확장을 고려해 `redemptions`(교환 내역) 테이블은 별도로 분리해두는 게 좋음.

**반별 통장 화면은 "오늘"만 보여준다 (2026-08-11 결정)**
- 수업 중 화면에는 **오늘 적립분만** 크게 표시. 누적 총액은 기본 숨김.
- 이유: 누적을 항상 띄우면 격차가 고정된 사실처럼 보여서 뒤처진 학생의 의욕이 꺾임.
  매일 0에서 시작하면 오늘 하루만큼은 누구나 1등이 가능함.
- 선생님용 **"누적 보기" 토글**은 남겨둠 (학생들 앞에서는 꺼두는 용도).
- **"오늘 마감"** 버튼 = 그날 내역 확정 + 잠금. `settlements` 테이블에 (반, 날짜) 단위로 기록.
  - 중요: 포인트는 **버튼 누르는 즉시 transactions 에 저장**된다. 마감은 저장이 아니라 확정임.
    (초안 상태로 들고 있다가 저장 시점에 쓰는 방식은 수업 중 브라우저가 닫히면 통째로 날아가서 배제)
  - 실수하면 마감 취소 가능.
- 누적 결과는 **"결과 보기"** 화면으로 분리 (기간: 오늘/이번 주/이번 달/전체, 범위: 반별/학원 전체).
  - 기존 "순위 보기" 를 대체함. 적립/차감/합계를 나눠서 보여줌.

**게임은 "템플릿" 구조로 설계**
- `game_templates`: 게임 종류, 이름, 항목 리스트(JSON), 만든 선생님.
- 선생님이 항목을 자유롭게 추가/수정/삭제하고 저장해서 다음 수업에 재사용.
- 새로운 게임 종류를 계속 추가할 수 있도록 "플러그인"처럼 등록 가능한 구조 추천.

**디자인 컨셉 — v2: 알록달록 & 귀여운 톤으로 전환 (2026-08-22)**
- 초등 교육 대상(선생님+어린 학생)이라는 피드백에 따라 "은행" 은유는 유지하되
  무겁고 격식 있던 톤(남색/세리프/펀치무늬)에서 밝고 둥근 톤으로 재설계함.
  참고 사이트: class5.co.kr (초등 영어학원 대상 서비스, 밝은 색+둥근 버튼 톤).
- **색상**: 기존 `--navy-*` 등 변수 이름은 그대로 두고 **값만** 하늘색/해바라기옐로우/민트/코랄
  계열로 교체 (코드 수정 최소화, CSS 변수 하나 바꾸면 전체 화면에 반영되는 구조 덕분에 가능했음).
  룰렛 색상(`app/src/lib/wheel.ts`)도 사탕색 팔레트로 별도 교체.
- **폰트**: Fraunces(세리프) → Jua(둥근 한글 디스플레이 폰트), Inter → Gowun Dodum(둥근 한글 본문),
  IBM Plex Mono(장부용 모노스페이스) → Fredoka(통통한 숫자 폰트). 이름은 그대로 유지하고
  `@import`만 바꿔서, 어차피 font-family 문자열 40여 곳을 다 고치지 않고
  실제 로드되는 폰트만 스왑하는 방식이 아니라 **문자열 자체를 전부 치환**했음
  (별칭 트릭 대신 정직하게 replace_all — 나중에 읽는 사람이 헷갈리지 않게).
- **radius**: 기본 14px → 22px (카드 더 둥글게). 브랜드 마크는 ₩ 기호 → 🐷(돼지 저금통) 이모지로
  교체 (저축 개념 + 귀여움 둘 다 잡음). 헤더 장식은 사선 줄무늬(보안지 느낌) → 동글동글 물방울 패턴.
- **가독성**: 전체 폰트 크기를 한 단계씩 키움(대략 +2px, 표시 숫자는 더 크게). 보조 텍스트 색
  (`--ink-soft`)과 헤더 배경 위 흰 글씨 대비도 WCAG AA 기준으로 재조정함(대비 계산 스크립트로 검증).
- ⚠️ 폰트 크기 일괄 변경 시 `sed`의 여러 `-e` 규칙을 연쇄 적용하면 이미 바뀐 값이 다음 규칙에
  또 걸려서 눈덩이처럼 불어나는 사고가 났었음(11px가 23px가 되는 식). 값 하나하나를
  원본 기준으로 한 번에 계산해서 라인 번호로 직접 치환하는 스크립트로 고쳐 잡음.
  **폰트 크기를 일괄 조정할 일이 또 생기면 이 방식(원본 값 스냅샷 → 한 번의 함수로 신규값 계산)을 쓸 것.**

**기술 스택 (확정됨)**
- 프론트: **React 19 + Vite + TypeScript + react-router** (`app/`)
- 백엔드/DB: **Supabase** (Postgres + Auth + RLS)
- 배포: Vercel/Netlify 예정 (아직 안 함)

> 단일 HTML 유지 vs React 마이그레이션을 놓고 검토했고, **React 로 결정**함.
> 이유는 UX 차이가 아니라 유지보수 때문. 게임 3종 + 3단 권한 + 멀티테넌시 + 추후 상품교환까지
> 얹으면 단일 HTML 파일은 6개월 내에 다시 갈아엎어야 할 가능성이 높다고 판단.

**권한/온보딩 플로우 (확정)**
- 프로필 생성 경로를 3개 RPC 로만 제한 (아무나 남의 학원에 owner 로 끼어드는 것 방지)
  - 원장: `create_academy` — 학원 + 기본 반 + 기본 프리셋 5개 자동 생성
  - 선생님: `join_academy_as_teacher(초대코드 6자리)`
  - 학생: `claim_student(학생코드 8자리)` — 학생 행에 본인 계정을 1:1 연결
- 학생은 본인 거래 내역만 조회 가능. 순위는 이름+합계만 반환하는 SECURITY DEFINER 함수로 별도 제공
  (남의 "수업 방해 -3" 같은 사유가 노출되지 않도록)

## 다음에 할 일 (우선순위 순)
1. **시한폭탄 / 타이머 맞추기 라이브 테스트** — 선생님 계정으로 `/games/bomb`, `/games/timer` 들어가서
   만들기/참가자 편집/설정(폭발 시간 범위, 목표 시간)/모드 토글까지 실제로 눌러서 확인 (Claude는
   로그인이 필요한 화면이라 직접 클릭 테스트를 못 했음 — 빌드는 통과, 효과음 합성과 시간 포맷
   로직은 브라우저 콘솔·Node로 따로 검증만 해둔 상태). 마이그레이션 불필요 — `game_type`에 CHECK
   제약이 없어 SQL 안 돌려도 바로 씀.
2. 단어를 이용한 게임, 문장 만들기 게임 (요청은 받았지만 아직 설계 전).
3. **플랫폼 관리자 기능 마무리** — `supabase/007_platform_admin.sql`을 Supabase SQL Editor에서
   실행하고, `likesea85@naver.com`(비밀번호 6자 이상)으로 정상 회원가입 → 온보딩에서
   "관리자로 시작하기" 클릭 → `/admin`에서 학원 목록/삭제/비밀번호 변경 동작 확인. (회원가입은
   본인이 직접 해야 함 — Claude는 계정 생성/비밀번호 입력을 대신 할 수 없음.)
4. 선생님별 담당 반 제한 (현재는 학원 내 모든 반 접근 가능 — 소규모 학원엔 오히려 편해서 일단 보류).
5. 배포 후 사용량 확대 시: 학부모 카카오톡/문자 알림, 학원별/계정별 사용내역(유료화 대비) — 둘 다
   "나중에 생각해보자"로 보류 중.

## 참고 파일
- `README.md` — **설치/실행/스키마 적용 순서. 여기부터 볼 것.**
- `supabase/schema.sql` — DB 스키마 + RLS 정책 + 온보딩 RPC.
- `point-bank-prototype.html` — 원본 프로토타입. 이제 앱에서 쓰이지 않지만 디자인 레퍼런스로 남겨둠.
