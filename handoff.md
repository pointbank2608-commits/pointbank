# 프로젝트 핸드오프 — 학원용 포인트 뱅킹 & 미니게임 웹앱

## 한 줄 요약
공부방/학원에서 쓸 웹앱. (1) 학생별 포인트 계좌를 만들어 선생님이 지급/차감하고 순위를 보여주는 "포인트 뱅킹" 서비스, (2) 룰렛/사다리/빙고 같은 미니게임 서비스, 이 두 개를 하나의 사이트 안에서 쓸 수 있게 만드는 것이 목표.

## 지금까지 진행된 것
- ~~포인트 뱅킹 HTML 프로토타입~~ → **React + Vite + Supabase 실제 서비스로 전환 완료** (`app/` 폴더).
  - 이메일/비밀번호 로그인, 원장/선생님/학생 3역할, 학원·반 단위 데이터 분리(RLS), 거래 원장 기반 포인트 지급/차감, 반별·학원 전체 순위까지 동작.
  - `point-bank-prototype.html` 은 참고용으로만 남겨둠. 디자인 톤은 그대로 계승했음.
- **남은 필수 작업 1개**: `supabase/schema.sql` 을 Supabase 대시보드 SQL Editor 에서 실행해야 함. 아직 안 돌렸음 (README 참고).
- **미니게임 1호: 돌림판(룰렛) 구현 완료** (2026-08-17). 사다리/빙고는 아직.
  - `game_templates` 테이블 그대로 사용 (스키마 변경 없음). `game_type='wheel'`, `items: {id,label}[]`.
  - 반 전용(`class_id` 지정) / 학원 전체 공용(`class_id = null`) 두 범위 지원. 만들 때 선생님이 선택.
  - 선생님·원장만 항목 추가/삭제/이름변경/삭제 가능(RLS `is_staff()` 그대로 적용됨, 추가 정책 불필요).
  - 학생도 접근 가능 — 보되 수정은 못 함. 네비게이션에 "돌림판" 추가.
  - SVG로 직접 그림(라이브러리 없음). 회전각 계산은 "12시=0도, 시계방향" 각도계 기준으로
    누적 회전값에 델타를 더하는 방식이라 여러 번 돌려도 자연스럽게 이어짐 (0으로 스냅 안 됨).
    `app/src/lib/wheel.ts` 의 `computeSpinRotation` 참고. 여러 항목수 x 여러 목표값으로
    시뮬레이션 검증 완료.
  - "당첨된 항목 제거하며 진행" 토글 — 저장된 템플릿을 건드리지 않고 그 판(session)에서만
    항목을 빼는 방식. 발표자 뽑기처럼 중복 없이 뽑아야 하는 용도.

## 요구사항 (원래 요청 원문 기준)

### 1. 학원용 포인트 뱅킹 서비스
- 숙제/수업태도 등에 대한 보상으로 선생님이 학생에게 포인트(가짜 화폐 개념)를 지급.
- 기존엔 종이 쿠폰으로 줘서 분실/집계가 번거로웠음 → 은행 시스템처럼 학생마다 "계좌"를 만들어 관리.
- 선생님이 수업 중/후에 포인트를 지급하거나 차감할 수 있어야 함.
- 포인트의 **단위 이름은 학원마다 커스터마이징 가능**해야 함 (예: 별, 달러 등).
- **"총 합계" 버튼**을 누르면 학생별 포인트 합산 후 **순위**를 보여줘야 함.
- **학원별, 반별로 데이터가 구분**되어야 함 (멀티테넌시).

### 2. 미니게임 서비스
- 돌림판(룰렛), 사다리 게임, 빙고 등.
- 각 게임의 **내용(항목)을 선생님이 입력하고 수정**할 수 있어야 함.
- 게임 종류는 **수시로 추가/제거**될 수 있어야 함 (확장 가능한 구조로 설계 필요).

### 3. 통합
- 위 두 서비스를 **하나의 사이트 안에서** 이용할 수 있어야 함.

## 확정된 설계 방향

**역할 구조**: 원장(전체 관리) / 선생님(자기 반 담당) / 학생(선택적 로그인, 조회 위주)

**데이터 모델 (계층 구조)**
```
학원(Academy)
 └ 반(Class)
    └ 학생(Student) — 계좌(Account)
 └ 선생님(Teacher)
```

**포인트 뱅킹은 "거래 원장(ledger)" 방식으로 설계**
- 잔액을 따로 저장하지 않고, 모든 지급/차감 트랜잭션(`transactions`)의 합으로 잔액을 계산.
- 트랜잭션 필드: 금액(delta), 사유, 지급한 선생님, 시각, 반 정보.
- 이유: 나중에 "누가 언제 왜 줬는지" 추적/정정 가능해야 하기 때문.
- 추후 "포인트로 상품 교환" 기능 확장을 고려해 `redemptions`(교환 내역) 테이블은 별도로 분리해두는 게 좋음.

**반별 통장 화면은 "오늘"만 보여준다 (2026-08-11 결정)**
- 수업 중 화면에는 **오늘 적립분만** 크게 표시. 누적 총액은 기본 숨김.
- 이유: 누적을 항상 띄우면 격차가 고정된 사실처럼 보여서 뒤처진 학생의 의욕이 꺾임.
  매일 0에서 시작하면 오늘 하루만큼은 누구나 1등이 가능함.
- 선생님용 **"누적 보기" 토글**은 남겨둠 (학생들 앞에서는 꺼두는 용도).
- **"오늘 마감"** 버튼 = 그날 내역 확정 + 잠금. `settlements` 테이블에 (반, 날짜) 단위로 기록.
  - 중요: 포인트는 **버튼 누르는 즉시 transactions 에 저장**된다. 마감은 저장이 아니라 확정임.
    (초안 상태로 들고 있다가 저장 시점에 쓰는 방식은 수업 중 브라우저가 닫히면 통째로 날아가서 배제)
  - 실수하면 마감 취소 가능.
- 누적 결과는 **"결과 보기"** 화면으로 분리 (기간: 오늘/이번 주/이번 달/전체, 범위: 반별/학원 전체).
  - 기존 "순위 보기" 를 대체함. 적립/차감/합계를 나눠서 보여줌.

**게임은 "템플릿" 구조로 설계**
- `game_templates`: 게임 종류, 이름, 항목 리스트(JSON), 만든 선생님.
- 선생님이 항목을 자유롭게 추가/수정/삭제하고 저장해서 다음 수업에 재사용.
- 새로운 게임 종류를 계속 추가할 수 있도록 "플러그인"처럼 등록 가능한 구조 추천.

**디자인 컨셉**
- "뱅킹"이라는 요청 자체가 은행 통장(passbook) 은유를 담고 있어서, 프로토타입은 실제 은행 통장 느낌(천공 무늬, 도장 찍는 애니메이션)으로 디자인함.
- 색상: 남색(네이비, 은행 커버) + 골드(포인트 스탬프 강조) + 세이지그린(적립)/브릭레드(차감).
- 폰트: 헤딩은 세리프(Fraunces), 본문은 Inter, 숫자/원장은 모노스페이스(IBM Plex Mono) — 통장에 찍히는 인쇄 숫자 느낌.
- 이 톤을 게임 서비스에도 이어가면 두 서비스가 하나의 사이트처럼 자연스럽게 느껴질 것.

**기술 스택 (확정됨)**
- 프론트: **React 19 + Vite + TypeScript + react-router** (`app/`)
- 백엔드/DB: **Supabase** (Postgres + Auth + RLS)
- 배포: Vercel/Netlify 예정 (아직 안 함)

> 단일 HTML 유지 vs React 마이그레이션을 놓고 검토했고, **React 로 결정**함.
> 이유는 UX 차이가 아니라 유지보수 때문. 게임 3종 + 3단 권한 + 멀티테넌시 + 추후 상품교환까지
> 얹으면 단일 HTML 파일은 6개월 내에 다시 갈아엎어야 할 가능성이 높다고 판단.

**권한/온보딩 플로우 (확정)**
- 프로필 생성 경로를 3개 RPC 로만 제한 (아무나 남의 학원에 owner 로 끼어드는 것 방지)
  - 원장: `create_academy` — 학원 + 기본 반 + 기본 프리셋 5개 자동 생성
  - 선생님: `join_academy_as_teacher(초대코드 6자리)`
  - 학생: `claim_student(학생코드 8자리)` — 학생 행에 본인 계정을 1:1 연결
- 학생은 본인 거래 내역만 조회 가능. 순위는 이름+합계만 반환하는 SECURITY DEFINER 함수로 별도 제공
  (남의 "수업 방해 -3" 같은 사유가 노출되지 않도록)

## 다음에 할 일 (우선순위 순)
1. ~~포인트 뱅킹에 실제 백엔드 연결~~ ✅ 완료. **단, `supabase/schema.sql` 실행은 아직 안 됨 → 이것부터.**
2. 미니게임 서비스 설계 및 프로토타입 (룰렛 → 사다리 → 빙고 순). `game_templates` 테이블은 준비돼 있음.
3. 게임 화면을 `app/src/pages/` 에 추가하고 상단 네비에 붙이면 통합 완료 (레이아웃은 `AppLayout.tsx`).
4. 선생님별 담당 반 제한 (현재는 학원 내 모든 반 접근 가능 — 소규모 학원엔 오히려 편해서 일단 보류).
5. 배포.

## 참고 파일
- `README.md` — **설치/실행/스키마 적용 순서. 여기부터 볼 것.**
- `supabase/schema.sql` — DB 스키마 + RLS 정책 + 온보딩 RPC.
- `point-bank-prototype.html` — 원본 프로토타입. 이제 앱에서 쓰이지 않지만 디자인 레퍼런스로 남겨둠.
